# Playing Futures — Claude Code Notes

## Hidden pages

The following pages exist in the content directory but are intentionally **not listed in the navigation menu** (`hugo.toml`):

- **Call for Papers** — `content/call-for-papers.md` (url: `/call-for-papers/`)
- **Archive** — `content/conferences/_index.md` (url: `/conferences/`)
- **Reviews 2026** — `content/reviews-2026.md` (url: `/reviews-2026/`) — internal page, linked from other pages only

They were removed from the `[menu]` section in `hugo.toml` but the content files are intact. To re-enable them, add their menu entries back to `hugo.toml`.
